"""shortbot: an MNQ day-trading bot built for Topstep's rules (short setups plus a
basic two-way setup).

The strategy (strategy.py) is shared by the backtester (backtest.py) and the
live TopstepX runner (live.py), so what is tested is exactly what trades.
"""

__version__ = "0.1.0"
